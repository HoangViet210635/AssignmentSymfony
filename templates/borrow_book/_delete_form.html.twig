<form method="post" action="{{ path('app_borrow_book_delete', {'id': borrow_book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ borrow_book.id) }}">
    <button class="btn" id="delete">Delete</button>
</form>
