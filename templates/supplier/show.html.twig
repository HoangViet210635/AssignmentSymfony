{% extends 'base.html.twig' %}

{% block title %}Supplier{% endblock %}

{% block body %}
    <h1>Supplier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ supplier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ supplier.name }}</td>
            </tr>
        </tbody>
    </table>

    <button><a href="{{ path('app_supplier_index') }}" style="text-decoration: none;">Back to list</a></button>

    <button><a href="{{ path('app_supplier_edit', {'id': supplier.id}) }}" style="text-decoration: none;">Edit</a></button>

    {{ include('supplier/_delete_form.html.twig') }}
{% endblock %}
